/**
 * Set AUTH_VISIBLE to true to show Login / Sign up in Navbar and Footer.
 * When false, auth links are hidden and /login, /signup still work if opened directly.
 */
export const AUTH_VISIBLE = true;
